import { SavedJobsView } from 'src/sections/dashboard/applicant/saved-jobs-view';

export const metadata = { title: 'Saved Jobs | Dashboard' };

export default function Page() {
  return <SavedJobsView />;
}
