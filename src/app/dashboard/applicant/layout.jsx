import { ApplicantRoleLayoutClient } from './applicant-role-layout-client';

export default function Layout({ children }) {
  return <ApplicantRoleLayoutClient>{children}</ApplicantRoleLayoutClient>;
}
